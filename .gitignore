# dependencies
node_modules/

# build output
dist/
dist-ssr/

# local env — NEVER commit secrets. RPC / WalletConnect keys live in the host's
# environment variables (Vercel), not in the repo.
.env.local
.env.*.local
.env

# editor / OS / tooling
.DS_Store
*.log
.vscode/*
!.vscode/extensions.json
.idea/
.vercel
*.tsbuildinfo

# --- secrets (managed by ID Agents Control Center) ---
.env
.env.*
!.env.example
!.env.sample
!.env.template
*.pem
*.key
*.p12
*.pfx
*.keystore
id_rsa
id_ed25519
*.secret
secrets.json
service-account*.json
gha-creds-*.json
.netrc
.pgpass
# --- end secrets ---
